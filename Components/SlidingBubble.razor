<div class="fixed inset-0 pointer-events-none overflow-hidden z-0 select-none opacity-40">
    <div class="ambient-blob blob-1"></div>
    <div class="ambient-blob blob-2"></div>
    <div class="ambient-blob blob-3"></div>
</div>

<style>
    .ambient-blob {
        position: absolute;
        border-radius: 50%;
        filter: blur(80px);
        opacity: 0.6;
        animation: float-ambient 24s ease-in-out infinite alternate;
        will-change: transform;
    }

    .blob-1 {
        width: 380px;
        height: 380px;
        top: -100px;
        left: -80px;
        background: radial-gradient(circle, rgba(245, 158, 11, 0.45) 0%, rgba(239, 68, 68, 0.2) 60%, transparent 80%);
        animation-duration: 20s;
    }

    .blob-2 {
        width: 440px;
        height: 440px;
        bottom: -120px;
        right: -100px;
        background: radial-gradient(circle, rgba(99, 102, 241, 0.4) 0%, rgba(168, 85, 247, 0.25) 60%, transparent 80%);
        animation-duration: 28s;
        animation-delay: -5s;
    }

    .blob-3 {
        width: 320px;
        height: 320px;
        top: 45%;
        left: 55%;
        background: radial-gradient(circle, rgba(16, 185, 129, 0.35) 0%, rgba(6, 182, 212, 0.15) 60%, transparent 80%);
        animation-duration: 22s;
        animation-delay: -10s;
    }

    @@keyframes float-ambient {
        0% {
            transform: translate(0, 0) scale(1);
        }
        50% {
            transform: translate(40px, -30px) scale(1.1);
        }
        100% {
            transform: translate(-30px, 40px) scale(0.95);
        }
    }
</style>